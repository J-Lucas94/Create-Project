{{>_msg}}
<div class="table-responsive">
    <table class="table table-responsive">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>CPF</th>
                <th>Email</th>
                <th>CEP</th>
                <th>Estado</th>
                <th>Municipio</th>
                <th>Bairro</th>
                <th>Endereço</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            {{#each clientes}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.nome}}</td>
                <td>{{this.cpf}}</td>
                <td>{{this.email}}</td>
                <td>{{this.cep}}</td>
                <td>{{this.uf}}</td>
                <td>{{this.municipio}}</td>
                <td>{{this.bairro}}</td>
                <td>{{this.endereco}}</td>
                {{#if cliente}}<td>{{cliente.nome}}</td>{{/if}}
                <td>
                    <span class="d-none d-md-block">
                        <a href="/canil/editarcliente/{{this.id}}" class="btn btn-outline-primary btn-sm">Editar</a>
                        <a href="/canil/deletarcliente/{{this.id}}"
                            class="btn btn-outline-danger btn-sm apagar_rg">Apagar</a>
                    </span>

                    <div class="d-block d-md-none">
                        <div class="dropdown">
                            <a href="#" class="btn btn-secondary dropdown-toggle" role="button" id="dropdownMenuLink"
                                data-bs-toggle="dropdown" arial-expanded="false">Ações</a>

                            <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                                <li><a class="dropdown-item" href="/canil/editarcliente/{{this.id}}">Editar</a></li>
                                <li><a class="dropdown-item" href="/canil/deletarcliente/{{this.id}}">Apagar</a></li>
                            </ul>
                        </div>
                    </div>

                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    <div class="d-flex m-auto">
        <div class="form-group col-1 m-auto">
            <a href="/canil/clientes" class="btn btn-primary ms-1">Cadastrar</a>
        </div>
    </div>
</div>